select p.product_name , sum(o.unit) as unit from Products p join Orders o on p.product_id = o.product_id where extract(month from order_date) = 2 and year(order_date) = 2020 group by product_name,p.product_id having unit >= 100